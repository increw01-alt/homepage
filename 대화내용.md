# 한국상품권협회 홈페이지 GitHub 업로드 & Cloudflare Pages 배포 작업 기록

## 작업 환경
- 작업 폴더: `c:\Users\OK\Desktop\블로그이미지\26.05.04 한국상품권협회홈페이지`
- GitHub 저장소: https://github.com/increw01-alt/homepage.git
- 배포 플랫폼: Cloudflare Pages
- 도메인: koreagiftcard.co.kr

---

## 1. GitHub 업로드 (완료)

빈 GitHub 저장소(`increw01-alt/homepage`)에 로컬 폴더를 초기화 후 푸시했음.

```bash
git init
git branch -M main
git config user.email "increw01@gmail.com"
git config user.name "increw01-alt"
git add .
git commit -m "Initial commit: 한국상품권협회 홈페이지"
git remote add origin https://github.com/increw01-alt/homepage.git
git push -u origin main
```

- 첫 커밋: `ad57127`
- 중복 파일 삭제 커밋: `c8df861` (129개 `(1)` 접미사 파일 제거)

---

## 2. Cloudflare Pages에서 홈페이지가 안 열리는 문제 — 진단 결과

### 문제 1: 저장소 루트에 `index.html`이 없음 ⚠️ (가장 큰 원인)

저장소 구조:
```
/ (루트)
├── (1).bashrc, (1).htpasswd 등 cPanel 서버 파일 (현재는 삭제됨)
└── html/
    ├── index.html  ← 실제 사이트 진입점
    ├── introduce.html
    ├── css/, js/, img/, font/, favicon/
    └── ...
```

Cloudflare Pages는 기본적으로 저장소 **루트**에서 `index.html`을 찾는데, 실제 사이트는 `html/` 하위에 있어서 사이트가 열리지 않음.

**해결책 (둘 중 하나):**
- (A) **권장**: Cloudflare Pages 대시보드 → 프로젝트 → **Settings → Builds & deployments → Build configurations → Build output directory** 값을 `html`로 변경
- (B) `html/` 안의 모든 파일을 저장소 루트로 옮기고 다시 푸시

### 문제 2: PHP include 사용 — 서브 페이지 작동 안 함

서브 페이지 30여 개에서 PHP include를 사용 중:

```php
<?php include('navi.php') ?>
<?php include('footer.php') ?>
```

**영향받는 파일들:** `introduce.html`, `introduce-2.html`, `shoppingmall.html`, `installment_shoppingmall.html`, `support.html`, `qna.html`, `qna_detail.html`, `noti.html`, `noti_detail.html`, `template.html`, `백화점상품권-할부구매.html`, `소액결제-상품권-구매.html`, `신용카드-상품권-할부구매.html`, `컬쳐랜드-할부구매.html` 등

Cloudflare Pages는 **정적 호스팅**이므로 PHP를 실행할 수 없음. 결과:
- PHP 코드가 그대로 텍스트로 출력되거나
- navi(상단 메뉴)와 footer(하단)가 아예 표시되지 않음

`index.html`(메인)은 PHP를 사용하지 않아 문제 1만 해결하면 정상 표시됨.

**해결 방향:**
- JavaScript `fetch()`로 navi.html / footer.html을 동적 로드하도록 변경
- 또는 빌드 타임에 navi/footer를 각 HTML에 인라인으로 삽입

### 문제 3: `.htaccess` 무효화

`html/.htaccess`는 Apache 전용 설정이라 Cloudflare에서 무시됨. 메뉴 링크가 다음처럼 확장자 없이 되어있다면:

```html
<a href="/introduce">소개</a>
<a href="/qna">자주 묻는 질문</a>
```

Cloudflare에서 404 에러 발생. `.htaccess`의 URL rewrite 규칙을 Cloudflare Pages용 `_redirects` 파일로 변환 필요.

**예시 `_redirects` 파일:**
```
/introduce       /introduce.html       200
/introduce-2     /introduce-2.html     200
/shoppingmall    /shoppingmall.html    200
/support         /support.html         200
/qna             /qna.html             200
/noti            /noti.html            200
```

---

## 3. 현재 진행 상황

- [x] GitHub 업로드
- [x] 중복 `(1)` 파일 129개 삭제
- [ ] Cloudflare Pages **Build output directory** = `html` 설정 (사용자가 직접 해야 함)
- [ ] PHP include → JS fetch 또는 인라인으로 변환
- [ ] `_redirects` 파일 작성 (`.htaccess` 대체)

---

## 4. 다른 AI에게 물어볼 때 핵심 질문 예시

1. "Cloudflare Pages에서 정적 사이트의 `<?php include('navi.php')?>`를 어떻게 대체할 수 있나? JavaScript fetch로 `navi.html`을 동적 로드하는 코드 예시 줘."
2. "Apache `.htaccess`의 RewriteRule을 Cloudflare Pages `_redirects` 파일 문법으로 변환하는 방법은?"
3. "Cloudflare Pages에서 `Build output directory`를 변경하면 기존 배포가 어떻게 영향받는지?"
4. "정적 사이트에서 navi/footer 같은 공통 요소를 관리하는 베스트 프랙티스는? (PHP include 없이)"

---

## 5. 참고: 사이트 기술 스택

- **순수 정적 HTML/CSS/JS** + 일부 PHP include
- jQuery 3.7.1, GSAP 3.12.7 (ScrollTrigger)
- ChannelIO 채팅 플러그인 (pluginKey: `11aaed06-5686-491b-8e9b-5f8848a5b94d`)
- Paperlogy 한글 폰트 (woff)
- 네이버 사이트 인증 메타태그 포함
