# 상품권 시세 페이지 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 백화점 상품권의 오프라인 매매 시세(살때·팔때)를 거래소 11곳에서 매시간 수집해 `/상품권시세` 페이지로 노출한다.

**Architecture:** 파서를 `rate_parsers.py` 로 분리하고 수집·검증·렌더를 `collect_rates.py` 가 맡는다. 업체별 파서는 완전히 독립이라 한 곳이 깨져도 나머지는 그대로 나간다. 기존 핫딜 파이프라인(`collect_deals.py`)과는 파일·데이터·장애가 모두 분리된다.

**Tech Stack:** Python 3.11 표준 라이브러리만 (urllib, re, json, unittest). 외부 의존성 추가 없음 — GitHub Actions 에서 `pip install` 없이 돌아야 한다.

## Global Constraints

- **의존성 추가 금지.** `collect_deals.py` 와 동일하게 표준 라이브러리만 쓴다. BeautifulSoup·requests 사용 불가.
- **인코딩**: 파일은 UTF-8, 파일 쓰기는 `newline="\n"` 고정 (윈도우 로컬과 리눅스 Actions 가 교차 실행돼도 개행 diff 가 생기지 않게).
- **콘솔 출력**: `sys.stdout.reconfigure(encoding="utf-8")` 로 시작한다 (윈도우 cp949 에서 한글 깨짐 방지).
- **검증 실패 시 빈 칸.** 추정값·기본값으로 채우지 않는다.
- **용어 고정**: `buy` = 고객이 사는 값(업체 표기 "판매가"), `sell` = 고객이 파는 값(업체 표기 "매입가"). 항상 `buy > sell`.
- **액면가 기준 10만원권**(`FACE = 100000`).
- **상품권 종류**: `신세계 / 롯데 / 현대 / 갤러리아 / AK` 5종으로 정규화. 그 외(구두·주유·문화·이랜드·홈플러스 등)는 버린다.
- **요청 예의**: 업체당 1회, 순차, 요청 간 `time.sleep(1.0)`, 실패 시 재시도 없음. User-Agent 에 협회명과 연락처를 밝힌다.
- **비교 사이트 수집 금지**: sisego.com 등 남이 모아 놓은 데이터는 절대 읽지 않는다.
- 설계 근거는 [상품권시세-설계.md](상품권시세-설계.md) 참조.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `rate_parsers.py` (신규) | 업체별 HTML → 행 리스트 파싱. 11개 함수 + 레지스트리 |
| `collect_rates.py` (신규) | 수집(HTTP) · 검증 · rates.json 생성 · 프리렌더 |
| `tests/test_rate_parsers.py` (신규) | 픽스처 기반 파서 회귀 테스트 |
| `tests/fixtures/*.html` (신규) | 각 업체 시세표 축약 픽스처 |
| `html/상품권시세.html` (신규) | 페이지 (프리렌더 마커 + JS 렌더러) |
| `html/rates.json` (생성물) | 수집 결과 |
| `html/sitemap.xml` (수정) | 새 URL 등록 |
| `html/*.html` 17개 (수정) | 메뉴에 "상품권 시세" 추가 |
| `.github/workflows/collect-deals.yml` (수정) | 시세 수집 스텝 + 건강 점검 |

**파서를 `collect_rates.py` 에서 분리하는 이유**: 파서는 남의 사이트 구조에 종속돼 가장 자주 바뀌는 코드다. 수집·검증·렌더와 섞이면 사이트 하나 고칠 때마다 전체 파일을 건드리게 된다.

---

## Task 1: 파서 레지스트리 + 공통 검증 + 테스트 인프라

**Files:**
- Create: `rate_parsers.py`
- Create: `tests/test_rate_parsers.py`
- Create: `tests/fixtures/.gitkeep`

**Interfaces:**
- Produces:
  - `normalize_brand(name: str) -> str | None` — 원문 종류명을 5종 중 하나로 정규화, 아니면 None
  - `parse_money(s: str) -> int | None` — `"96,700원"` → `96700`
  - `parse_rate(s: str) -> float | None` — `"(3.30%)"` → `3.3`
  - `validate_row(row: dict, face: int) -> bool` — 4중 검증
  - `Row = {"brand": str, "buy": int|None, "buyRate": float|None, "sell": int|None, "sellRate": float|None}`
  - `PARSERS: dict[str, Callable[[str], list[Row]]]` — 업체키 → 파서 (Task 2~4 에서 채움)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_rate_parsers.py`:

```python
# -*- coding: utf-8 -*-
import os
import sys
import unittest

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import rate_parsers as rp


class TestHelpers(unittest.TestCase):
    def test_normalize_brand_maps_known_names(self):
        self.assertEqual(rp.normalize_brand("신세계상품권"), "신세계")
        self.assertEqual(rp.normalize_brand("신세계백화점 10만원권"), "신세계")
        self.assertEqual(rp.normalize_brand("롯데 백화점(10만원권)"), "롯데")
        self.assertEqual(rp.normalize_brand("현대백화점"), "현대")
        self.assertEqual(rp.normalize_brand("갤러리아백화점"), "갤러리아")
        self.assertEqual(rp.normalize_brand("AK플라자"), "AK")

    def test_normalize_brand_rejects_other_categories(self):
        self.assertIsNone(rp.normalize_brand("국민관광상품권"))
        self.assertIsNone(rp.normalize_brand("이랜드 상품권"))
        self.assertIsNone(rp.normalize_brand("주유상품권"))
        self.assertIsNone(rp.normalize_brand("홈플러스"))

    def test_normalize_brand_rejects_gift_variants(self):
        # "증정용"은 일반 시세와 조건이 달라 섞으면 안 된다
        self.assertIsNone(rp.normalize_brand("롯데 상품권★증정용"))
        self.assertIsNone(rp.normalize_brand("[사은증정]신세계"))

    def test_parse_money(self):
        self.assertEqual(rp.parse_money("96,700원"), 96700)
        self.assertEqual(rp.parse_money(" 96700 "), 96700)
        self.assertIsNone(rp.parse_money("—"))
        self.assertIsNone(rp.parse_money(""))

    def test_parse_rate(self):
        self.assertEqual(rp.parse_rate("(3.30%)"), 3.3)
        self.assertEqual(rp.parse_rate("3.8%"), 3.8)
        self.assertIsNone(rp.parse_rate("없음"))


class TestValidateRow(unittest.TestCase):
    FACE = 100000

    def test_accepts_consistent_row(self):
        row = {"brand": "신세계", "buy": 96700, "buyRate": 3.3,
               "sell": 96600, "sellRate": 3.4}
        self.assertTrue(rp.validate_row(row, self.FACE))

    def test_rejects_identity_mismatch(self):
        # 96,700원인데 할인율이 10%로 적혀 있으면 컬럼 정렬이 어긋난 것
        row = {"brand": "신세계", "buy": 96700, "buyRate": 10.0,
               "sell": 96600, "sellRate": 3.4}
        self.assertFalse(rp.validate_row(row, self.FACE))

    def test_rejects_out_of_range(self):
        row = {"brand": "신세계", "buy": 50000, "buyRate": 50.0,
               "sell": 49000, "sellRate": 51.0}
        self.assertFalse(rp.validate_row(row, self.FACE))

    def test_rejects_buy_not_greater_than_sell(self):
        # 매입/판매 컬럼을 반대로 읽은 경우
        row = {"brand": "신세계", "buy": 96600, "buyRate": 3.4,
               "sell": 96700, "sellRate": 3.3}
        self.assertFalse(rp.validate_row(row, self.FACE))

    def test_accepts_row_with_only_one_side(self):
        # 업체가 살때만 공시하는 경우도 있다
        row = {"brand": "롯데", "buy": 95700, "buyRate": 4.3,
               "sell": None, "sellRate": None}
        self.assertTrue(rp.validate_row(row, self.FACE))

    def test_rejects_row_with_no_price(self):
        row = {"brand": "롯데", "buy": None, "buyRate": None,
               "sell": None, "sellRate": None}
        self.assertFalse(rp.validate_row(row, self.FACE))

    def test_rejects_unknown_brand(self):
        row = {"brand": None, "buy": 96700, "buyRate": 3.3,
               "sell": 96600, "sellRate": 3.4}
        self.assertFalse(rp.validate_row(row, self.FACE))

    def test_accepts_row_without_rate(self):
        # 할인율 표기가 없는 업체는 금액만으로 범위 검사한다
        row = {"brand": "현대", "buy": 96800, "buyRate": None,
               "sell": 96600, "sellRate": None}
        self.assertTrue(rp.validate_row(row, self.FACE))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `python -m unittest tests.test_rate_parsers -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'rate_parsers'`

- [ ] **Step 3: 최소 구현**

`rate_parsers.py`:

```python
# -*- coding: utf-8 -*-
"""상품권 거래소별 시세 파서.

각 업체 파서는 HTML 문자열을 받아 Row 리스트를 돌려준다.
Row = {"brand", "buy", "buyRate", "sell", "sellRate"}
  buy  = 고객이 사는 값 (업체 표기 "판매가")
  sell = 고객이 파는 값 (업체 표기 "매입가")

파서는 남의 사이트 구조에 종속돼 가장 자주 깨지는 코드다.
새 파서를 추가하면 반드시 tests/fixtures 에 축약 픽스처와 테스트를 함께 넣을 것.
"""

import re

FACE = 100000  # 시세표 기준 액면가(10만원권)

# 표기 흔들림을 흡수해 5종으로 정규화한다.
# 순서 주의: "신세계백화점"이 "백화점"보다 먼저 잡혀야 한다.
_BRAND_PATTERNS = [
    ("신세계", ["신세계", "SSG"]),
    ("롯데", ["롯데"]),
    ("현대", ["현대"]),
    ("갤러리아", ["갤러리아"]),
    ("AK", ["AK플라자", "AK백화점", "AK상품권", "애경", "AK"]),
]

# 일반 백화점 상품권 시세와 조건이 다른 항목은 제외한다.
_BRAND_EXCLUDE = ["증정", "사은", "구두", "주유", "문화", "국민", "관광",
                  "이랜드", "홈플러스", "교육", "여행", "레저", "기프트카드"]


def normalize_brand(name):
    """원문 종류명을 5종 중 하나로. 해당 없으면 None."""
    if not name:
        return None
    s = re.sub(r"\s+", "", str(name))
    if any(x in s for x in _BRAND_EXCLUDE):
        return None
    for canon, keys in _BRAND_PATTERNS:
        if any(k in s for k in keys):
            return canon
    return None


def parse_money(s):
    """'96,700원' → 96700. 숫자가 없으면 None."""
    if s is None:
        return None
    m = re.search(r"(\d[\d,]{2,})", str(s))
    if not m:
        return None
    return int(m.group(1).replace(",", ""))


def parse_rate(s):
    """'(3.30%)' → 3.3. 없으면 None."""
    if s is None:
        return None
    m = re.search(r"(\d+(?:\.\d+)?)\s*%", str(s))
    if not m:
        return None
    return float(m.group(1))


def validate_row(row, face=FACE):
    """4중 검증. 하나라도 실패하면 이 행은 버린다."""
    if not row.get("brand"):
        return False

    buy, sell = row.get("buy"), row.get("sell")
    if buy is None and sell is None:
        return False

    for price, rate in ((buy, row.get("buyRate")), (sell, row.get("sellRate"))):
        if price is None:
            continue
        # 범위: 액면가의 90~102%
        if not (face * 0.90 <= price <= face * 1.02):
            return False
        # 항등식: 금액 ≈ 액면가 × (1 − 할인율/100), 오차 액면가의 1%
        if rate is not None:
            expected = face * (1 - rate / 100.0)
            if abs(price - expected) > face * 0.01:
                return False

    # 부등식: 둘 다 있으면 살때 > 팔때 (업체 마진)
    if buy is not None and sell is not None and buy <= sell:
        return False

    return True


# 업체키 → 파서 함수. Task 2~4 에서 채운다.
PARSERS = {}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m unittest tests.test_rate_parsers -v`
Expected: PASS (16 tests)

- [ ] **Step 5: 커밋**

```bash
git add rate_parsers.py tests/test_rate_parsers.py tests/fixtures/.gitkeep
git commit -m "Rates: 파서 공통 기반 — 종류 정규화·금액/할인율 파싱·4중 검증"
```

---

## Task 2: 파서 3곳 (월드티켓 · 엑스이 · 최고상품권)

가장 단순한 구조 3곳으로 파이프라인을 먼저 검증한다.

**Files:**
- Modify: `rate_parsers.py` (파서 3개 + PARSERS 등록)
- Modify: `tests/test_rate_parsers.py` (파서 테스트 추가)
- Create: `tests/fixtures/worldticket.html`, `tests/fixtures/xegift.html`, `tests/fixtures/choigo.html`

**Interfaces:**
- Consumes: `normalize_brand`, `parse_money`, `parse_rate`, `validate_row` (Task 1)
- Produces: `parse_worldticket(html) -> list[Row]`, `parse_xegift(html) -> list[Row]`, `parse_choigo(html) -> list[Row]`, `PARSERS` 에 `worldticket`/`xegift`/`choigo` 키

- [ ] **Step 1: 실제 HTML 을 받아 픽스처 만들기**

```bash
UA="Mozilla/5.0 (compatible; KoreaGiftcardBot/1.0; +https://koreagiftcard.co.kr)"
curl -sL -A "$UA" "https://www.myeongdongworldticket.co.kr/" -o /tmp/wt.html
curl -sL -A "$UA" "http://xegift.co.kr/html/sub0101.php" -o /tmp/xe.html
curl -sL -A "$UA" "https://www.choigoticket.com/html/sub0101.php" -o /tmp/cg.html
```

각 파일에서 **시세표 부분만** 잘라 `tests/fixtures/` 에 저장한다. 페이지 전체를 저장하지 않는다 — 남의 사이트 전체를 저장소에 넣지 않기 위함이고, 픽스처는 파싱 대상 구조만 있으면 충분하다.

- 월드티켓: `<div class="guide-table">` … `</div>` 구간
- 엑스이: `<table>` … `</table>` 구간 (시세표 1개)
- 최고상품권: `<table class="tbl price_tbl">` … `</table>` 구간

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/test_rate_parsers.py` 에 추가 (import 아래):

```python
FIXTURES = os.path.join(os.path.dirname(os.path.abspath(__file__)), "fixtures")


def load_fixture(name):
    with open(os.path.join(FIXTURES, name), encoding="utf-8") as f:
        return f.read()


def rows_by_brand(rows):
    return {r["brand"]: r for r in rows}


class TestParserContract(unittest.TestCase):
    """모든 파서가 공통으로 지켜야 할 계약."""

    CASES = [
        ("worldticket.html", "worldticket"),
        ("xegift.html", "xegift"),
        ("choigo.html", "choigo"),
    ]

    def test_parsers_return_valid_rows(self):
        for fixture, key in self.CASES:
            with self.subTest(shop=key):
                rows = rp.PARSERS[key](load_fixture(fixture))
                self.assertGreater(len(rows), 0, f"{key}: 행이 0개")
                for r in rows:
                    self.assertIn(r["brand"], ["신세계", "롯데", "현대", "갤러리아", "AK"])
                    self.assertTrue(rp.validate_row(r), f"{key}: 검증 실패 {r}")

    def test_parsers_cover_major_brands(self):
        # 백화점 3사는 어느 업체나 취급한다
        for fixture, key in self.CASES:
            with self.subTest(shop=key):
                brands = {r["brand"] for r in rp.PARSERS[key](load_fixture(fixture))}
                self.assertTrue({"신세계", "롯데", "현대"} <= brands,
                                f"{key}: 3사 누락 {brands}")

    def test_parsers_return_empty_on_garbage(self):
        # 사이트가 개편돼 구조가 사라지면 빈 리스트여야 한다(예외 대신)
        for _, key in self.CASES:
            with self.subTest(shop=key):
                self.assertEqual(rp.PARSERS[key]("<html><body>없음</body></html>"), [])

    def test_buy_greater_than_sell(self):
        for fixture, key in self.CASES:
            with self.subTest(shop=key):
                for r in rp.PARSERS[key](load_fixture(fixture)):
                    if r["buy"] is not None and r["sell"] is not None:
                        self.assertGreater(r["buy"], r["sell"], f"{key}: {r}")
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `python -m unittest tests.test_rate_parsers -v`
Expected: FAIL — `KeyError: 'worldticket'`

- [ ] **Step 4: 파서 구현**

`rate_parsers.py` 에 추가. 조사에서 확인된 구조는 아래와 같다. **구현 전 픽스처를 직접 열어 구조를 눈으로 확인하고, 아래 구조와 다르면 픽스처를 따른다.**

```python
_TAG = re.compile(r"<[^>]+>")
_WS = re.compile(r"[\s​\xa0]+")


def _text(html_fragment):
    """태그 제거 후 공백 정규화."""
    s = _TAG.sub(" ", html_fragment)
    s = s.replace("&nbsp;", " ").replace("&amp;", "&")
    return _WS.sub(" ", s).strip()


def _rows_from_table(html, cols):
    """<tr> 를 순회하며 지정한 컬럼 인덱스에서 값을 뽑는 공통 헬퍼.

    cols = {"name": 0, "sell": 1, "buy": 2} 처럼 td 인덱스를 넘긴다.
    (업체 표기 "매입가"=sell, "판매가"=buy 이므로 호출부에서 맞춰 넘길 것)
    """
    out = []
    for tr in re.findall(r"<tr[^>]*>(.*?)</tr>", html, re.S | re.I):
        tds = re.findall(r"<t[dh][^>]*>(.*?)</t[dh]>", tr, re.S | re.I)
        if len(tds) <= max(cols.values()):
            continue
        brand = normalize_brand(_text(tds[cols["name"]]))
        if not brand:
            continue
        sell_cell = _text(tds[cols["sell"]]) if "sell" in cols else ""
        buy_cell = _text(tds[cols["buy"]]) if "buy" in cols else ""
        row = {
            "brand": brand,
            "buy": parse_money(buy_cell), "buyRate": parse_rate(buy_cell),
            "sell": parse_money(sell_cell), "sellRate": parse_rate(sell_cell),
        }
        if validate_row(row):
            out.append(row)
    return out


def parse_worldticket(html):
    """월드티켓상품권 — div.guide-table 안 table, td[0]=명 td[1]=매입 td[2]=판매."""
    m = re.search(r'<div[^>]*class="[^"]*guide-table[^"]*"[^>]*>(.*?)</div>', html, re.S | re.I)
    scope = m.group(1) if m else html
    return _rows_from_table(scope, {"name": 0, "sell": 1, "buy": 2})


def parse_xegift(html):
    """엑스이상품권 — 단일 table, td[0]=명 td[1]=매입(.blue) td[2]=판매(.red)."""
    return _rows_from_table(html, {"name": 0, "sell": 1, "buy": 2})


def parse_choigo(html):
    """최고상품권 — 수량 input 의 커스텀 속성에서 직접 추출.

    <input _amt1="96600" _amt2="96800" _item="현대 상품권 (10만원)">
      _amt1 = 매입가(sell), _amt2 = 판매가(buy)
    액면가 10만원권 행만 취한다.
    """
    out = []
    for m in re.finditer(
            r'<input[^>]*_amt1="(\d+)"[^>]*_amt2="(\d+)"[^>]*_item="([^"]+)"', html, re.I):
        sell, buy, item = int(m.group(1)), int(m.group(2)), m.group(3)
        if "10만" not in item.replace(" ", ""):
            continue
        brand = normalize_brand(item)
        if not brand:
            continue
        row = {"brand": brand, "buy": buy, "buyRate": None,
               "sell": sell, "sellRate": None}
        if validate_row(row):
            out.append(row)
    return out


PARSERS.update({
    "worldticket": parse_worldticket,
    "xegift": parse_xegift,
    "choigo": parse_choigo,
})
```

주의: `_rows_from_table` 은 액면가를 구분하지 않는다. 업체가 5만·50만원권을 같은 표에 넣으면 `validate_row` 의 범위 검사(90~102%)가 걸러낸다 — 50만원권 483,000 은 범위 밖이고 5만원권 48,300 도 범위 밖이다. **이 필터가 액면가 선별을 겸한다.**

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m unittest tests.test_rate_parsers -v`
Expected: PASS

실패하면 픽스처를 열어 실제 td 순서를 확인하고 `cols` 인덱스를 고친다. 조사 결과보다 **픽스처가 항상 우선**이다.

- [ ] **Step 6: 커밋**

```bash
git add rate_parsers.py tests/
git commit -m "Rates: 파서 3곳(월드티켓·엑스이·최고상품권) + 픽스처 회귀 테스트"
```

---

## Task 3: 파서 4곳 (미래 · 명인 · 중앙 · 시티페이)

**Files:**
- Modify: `rate_parsers.py`, `tests/test_rate_parsers.py`
- Create: `tests/fixtures/meee.html`, `mingren.html`, `centralgift.html`, `citypay.html`

**Interfaces:**
- Consumes: Task 1 의 헬퍼, Task 2 의 `_rows_from_table`/`_text`
- Produces: `parse_meee`, `parse_mingren`, `parse_centralgift`, `parse_citypay` + PARSERS 등록

- [ ] **Step 1: 픽스처 생성**

```bash
UA="Mozilla/5.0 (compatible; KoreaGiftcardBot/1.0; +https://koreagiftcard.co.kr)"
curl -sL -A "$UA" "https://www.meee.co.kr/" -o /tmp/meee.html
curl -sL -A "$UA" "https://www.mingren.co.kr/" -o /tmp/mingren.html
curl -sL -A "$UA" "https://centralgift.imweb.me/" -o /tmp/central.html
curl -sL -A "$UA" "https://city-pay.co.kr/" -o /tmp/citypay.html
```

시세표 구간만 잘라 픽스처로 저장한다. **명인상품권은 반드시 `div.sise_box.box0.active` 구간만** 저장한다 — box0~box6 에 같은 상품명이 중복 등장하므로, 픽스처에 다른 박스가 섞이면 테스트가 실제 위험(잘못된 값 채택)을 놓친다.

- [ ] **Step 2: 테스트에 4곳 추가**

`TestParserContract.CASES` 리스트에 추가:

```python
    CASES = [
        ("worldticket.html", "worldticket"),
        ("xegift.html", "xegift"),
        ("choigo.html", "choigo"),
        ("meee.html", "meee"),
        ("mingren.html", "mingren"),
        ("centralgift.html", "centralgift"),
        ("citypay.html", "citypay"),
    ]
```

그리고 명인상품권 전용 회귀 테스트를 추가한다:

```python
class TestMingrenScope(unittest.TestCase):
    def test_ignores_inactive_boxes(self):
        """box0 이외의 박스에 있는 값을 주워오면 안 된다.

        명인상품권 페이지에는 같은 상품명이 여러 박스에 중복 등장한다.
        스코프를 좁히지 않으면 조용히 틀린 값이 들어간다.
        """
        html = (
            '<div class="sise_box box0 active">'
            '<table><tr><td>신세계상품권</td><td>96,600원(3.40%)</td>'
            '<td>96,700원(3.30%)</td></tr></table></div>'
            '<div class="sise_box box2">'
            '<table><tr><td>신세계상품권</td><td>90,000원(10.00%)</td>'
            '<td>91,000원(9.00%)</td></tr></table></div>'
        )
        rows = rp.parse_mingren(html)
        self.assertEqual(len(rows), 1)
        self.assertEqual(rows[0]["sell"], 96600)
        self.assertEqual(rows[0]["buy"], 96700)
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `python -m unittest tests.test_rate_parsers -v`
Expected: FAIL — `KeyError: 'meee'`, `AttributeError: parse_mingren`

- [ ] **Step 4: 파서 구현**

```python
def parse_meee(html):
    """미래상품권 — table tbody.giftcard_list > tr, td.tit=명, 매입/판매 순."""
    m = re.search(r'<tbody[^>]*class="[^"]*giftcard_list[^"]*"[^>]*>(.*?)</tbody>',
                  html, re.S | re.I)
    scope = m.group(1) if m else html
    return _rows_from_table(scope, {"name": 0, "sell": 1, "buy": 2})


def parse_mingren(html):
    """명인상품권 — div.sise_box.box0.active 로 스코프를 좁힌 뒤 표를 읽는다.

    box0~box6 에 같은 상품명이 중복 등장하므로 스코프 한정이 필수다.
    active 박스를 못 찾으면 빈 리스트를 돌려준다(전체 검색 금지).
    """
    m = re.search(
        r'<div[^>]*class="[^"]*sise_box[^"]*box0[^"]*active[^"]*"[^>]*>(.*?)(?=<div[^>]*class="[^"]*sise_box|\Z)',
        html, re.S | re.I)
    if not m:
        return []
    return _rows_from_table(m.group(1), {"name": 0, "sell": 1, "buy": 2})


def parse_centralgift(html):
    """중앙상품권 — 페이지 내 table 1개. td[0]=명 td[1]=액면 td[2]=매입 td[3]=판매."""
    return _rows_from_table(html, {"name": 0, "sell": 2, "buy": 3})


def parse_citypay(html):
    """시티페이 — wpDataTables. td[0]=종류(<br>로 액면가), td[1]=매입 td[2]=판매."""
    m = re.search(r'<table[^>]*id="table_\d+"[^>]*>(.*?)</table>', html, re.S | re.I)
    scope = m.group(1) if m else html
    return _rows_from_table(scope, {"name": 0, "sell": 1, "buy": 2})


PARSERS.update({
    "meee": parse_meee,
    "mingren": parse_mingren,
    "centralgift": parse_centralgift,
    "citypay": parse_citypay,
})
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m unittest tests.test_rate_parsers -v`
Expected: PASS

컬럼 인덱스가 틀리면 `validate_row` 가 전부 걸러 행이 0개가 된다. 그때는 픽스처의 `<tr>` 하나를 출력해 td 순서를 눈으로 확인하고 고친다.

- [ ] **Step 6: 커밋**

```bash
git add rate_parsers.py tests/
git commit -m "Rates: 파서 4곳(미래·명인·중앙·시티페이) — 명인 스코프 한정 회귀 테스트 포함"
```

---

## Task 4: 파서 4곳 (우천 · 우리에스티 · 상품권백화점 · 고고)

EUC-KR 2곳과 비표 구조 2곳.

**Files:**
- Modify: `rate_parsers.py`, `tests/test_rate_parsers.py`
- Create: `tests/fixtures/wooticket.html`, `woorist.html`, `sgbaekhwajeom.html`, `gogo.html` (모두 UTF-8 로 변환해 저장)

**Interfaces:**
- Produces: `parse_wooticket`, `parse_woorist`, `parse_sgbaekhwajeom`, `parse_gogo` + PARSERS 등록

- [ ] **Step 1: 픽스처 생성 (EUC-KR 변환 주의)**

```bash
UA="Mozilla/5.0 (compatible; KoreaGiftcardBot/1.0; +https://koreagiftcard.co.kr)"
# EUC-KR 2곳 — 반드시 변환해서 저장
python -c "import urllib.request as u; r=u.Request('http://www.wooticket.com/', headers={'User-Agent':'$UA'}); open('/tmp/woo.html','w',encoding='utf-8').write(u.urlopen(r,timeout=20).read().decode('euc-kr','replace'))"
python -c "import urllib.request as u; r=u.Request('https://woorist.co.kr/happyshop/show_pricelist_woori.php', headers={'User-Agent':'$UA'}); open('/tmp/woorist.html','w',encoding='utf-8').write(u.urlopen(r,timeout=20).read().decode('euc-kr','replace'))"
curl -sL -A "$UA" "http://www.xn--zf0bt1zcnd5pj69p6qc.com/" -o /tmp/sgb.html
curl -sL -A "$UA" "https://xn--299aa03ct82dtjik1sz2c.com/" -o /tmp/gogo.html
```

우리에스티는 **`show_pricelist_woori.php`** 를 쓴다. 설계 조사에서 확인했듯 기존 `sub_list.html` 에는 살때만 있고 팔때가 없다.

- [ ] **Step 2: 테스트에 4곳 추가 + 인코딩 회귀 테스트**

`CASES` 에 4곳을 추가하고, 아래 테스트를 더한다:

```python
class TestEncoding(unittest.TestCase):
    def test_euckr_shops_parse_korean_brands(self):
        """EUC-KR 사이트를 UTF-8 로 잘못 읽으면 종류명이 깨져 행이 0개가 된다.

        픽스처는 이미 UTF-8 로 변환해 저장했으므로, 여기서 행이 나오면
        수집기(collect_rates.py)의 디코딩만 맞추면 된다.
        """
        for fixture, key in [("wooticket.html", "wooticket"), ("woorist.html", "woorist")]:
            with self.subTest(shop=key):
                rows = rp.PARSERS[key](load_fixture(fixture))
                self.assertGreater(len(rows), 0, f"{key}: 한글 종류명 매칭 실패")
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `python -m unittest tests.test_rate_parsers -v`
Expected: FAIL — `KeyError: 'wooticket'`

- [ ] **Step 4: 파서 구현**

```python
def parse_wooticket(html):
    """우천상품권 — 레거시 테이블 레이아웃. 전 품목이 한 표에 있다.

    페이지 인코딩이 EUC-KR 이므로 수집기에서 반드시 디코딩해 넘길 것.
    """
    return _rows_from_table(html, {"name": 0, "sell": 1, "buy": 2})


def parse_woorist(html):
    """우리에스티 — 매입시세표 전용 페이지(show_pricelist_woori.php). EUC-KR."""
    return _rows_from_table(html, {"name": 0, "sell": 1, "buy": 2})


def parse_sgbaekhwajeom(html):
    """상품권백화점 — 영카트 상품 목록이 곧 시세표.

    li.sct_li 블록마다 상품명·액면가·가격이 들어 있다.
    표가 아니므로 블록 단위로 훑는다.
    """
    out = []
    for block in re.findall(r'<li[^>]*class="[^"]*sct_li[^"]*"[^>]*>(.*?)</li>', html, re.S | re.I):
        name = _text(re.sub(r"<div[^>]*sct_basic.*", "", block, flags=re.S))
        brand = normalize_brand(name)
        if not brand or "10만" not in name.replace(" ", ""):
            continue
        nums = [int(n.replace(",", "")) for n in re.findall(r"(\d[\d,]{4,})", _text(block))]
        prices = [n for n in nums if FACE * 0.90 <= n <= FACE * 1.02]
        if not prices:
            continue
        row = {"brand": brand, "buy": max(prices), "buyRate": None,
               "sell": min(prices) if len(prices) > 1 else None, "sellRate": None}
        if len(prices) == 1:
            row["sell"] = None
        if validate_row(row):
            out.append(row)
    return out


def parse_gogo(html):
    """고고상품권 — div.tab-editor[editor-name] 6블록. 블록명이 곧 종류명이다.

    탭은 CSS 로 숨겨질 뿐 전 블록이 서버 HTML 에 있다.
    """
    out = []
    blocks = re.split(r'(?=<div[^>]*class="[^"]*tab-editor[^"]*"[^>]*editor-name=")', html)
    for b in blocks:
        m = re.search(r'editor-name="([^"]*)"', b)
        if not m:
            continue
        brand = normalize_brand(m.group(1))
        if not brand:
            continue
        rows = _rows_from_table(b, {"name": 0, "sell": 1, "buy": 2})
        # 블록 안 표의 종류명이 액면가만 담고 있을 수 있어, 블록명 기준으로 덮어쓴다
        for r in rows:
            r["brand"] = brand
        out.extend(rows)
    return out


PARSERS.update({
    "wooticket": parse_wooticket,
    "woorist": parse_woorist,
    "sgbaekhwajeom": parse_sgbaekhwajeom,
    "gogo": parse_gogo,
})
```

`parse_gogo` 의 `_rows_from_table` 은 `normalize_brand` 로 행을 거르므로, 표의 종류명이 "10만원권"처럼 브랜드가 없으면 행이 0개가 된다. 그 경우 블록 단위 파싱으로 바꾼다 — 픽스처를 보고 판단한다.

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m unittest tests.test_rate_parsers -v`
Expected: PASS (11개 업체 전부)

- [ ] **Step 6: 커밋**

```bash
git add rate_parsers.py tests/
git commit -m "Rates: 파서 4곳(우천·우리에스티·상품권백화점·고고) — EUC-KR 회귀 테스트 포함"
```

---

## Task 5: 수집기 `collect_rates.py`

**Files:**
- Create: `collect_rates.py`
- Create: `tests/test_collect_rates.py`

**Interfaces:**
- Consumes: `rate_parsers.PARSERS`, `validate_row`, `FACE`
- Produces:
  - `SHOPS: list[dict]` — `{key, name, url, area, encoding}`
  - `fetch(url, encoding) -> str`
  - `collect() -> (shops: list, health: dict)`
  - `build_summary(shops) -> list`
  - `html/rates.json`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_collect_rates.py`:

```python
# -*- coding: utf-8 -*-
import os
import sys
import unittest

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import collect_rates as cr


class TestShopRegistry(unittest.TestCase):
    def test_every_shop_has_a_parser(self):
        import rate_parsers as rp
        for s in cr.SHOPS:
            self.assertIn(s["key"], rp.PARSERS, f"{s['name']}: 파서 없음")

    def test_shop_fields(self):
        for s in cr.SHOPS:
            for f in ("key", "name", "url", "area", "encoding"):
                self.assertIn(f, s, f"{s.get('name')}: {f} 누락")
            self.assertTrue(s["url"].startswith("http"))

    def test_no_aggregator_sources(self):
        """비교 사이트를 수집원으로 넣으면 안 된다(남의 DB 복제)."""
        for s in cr.SHOPS:
            self.assertNotIn("sisego", s["url"])


class TestSummary(unittest.TestCase):
    def test_picks_cheapest_buy_and_highest_sell(self):
        shops = [
            {"name": "A", "rows": [{"brand": "신세계", "buy": 96800, "buyRate": 3.2,
                                     "sell": 96000, "sellRate": 4.0}]},
            {"name": "B", "rows": [{"brand": "신세계", "buy": 96200, "buyRate": 3.8,
                                     "sell": 96700, "sellRate": 3.3}]},
        ]
        summary = cr.build_summary(shops)
        s = {x["brand"]: x for x in summary}["신세계"]
        self.assertEqual(s["bestBuy"]["price"], 96200)   # 살 때는 싼 쪽
        self.assertEqual(s["bestBuy"]["shop"], "B")
        self.assertEqual(s["bestSell"]["price"], 96700)  # 팔 때는 비싼 쪽
        self.assertEqual(s["bestSell"]["shop"], "B")

    def test_skips_brand_with_no_data(self):
        shops = [{"name": "A", "rows": [{"brand": "롯데", "buy": 95700, "buyRate": 4.3,
                                          "sell": None, "sellRate": None}]}]
        summary = cr.build_summary(shops)
        s = {x["brand"]: x for x in summary}["롯데"]
        self.assertIsNotNone(s["bestBuy"])
        self.assertIsNone(s["bestSell"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m unittest tests.test_collect_rates -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'collect_rates'`

- [ ] **Step 3: 구현**

`collect_rates.py`:

```python
# -*- coding: utf-8 -*-
"""백화점 상품권 거래소 시세를 수집해 html/rates.json 과 프리렌더를 만든다.

핫딜 수집(collect_deals.py)과 완전히 분리돼 있다 — 한쪽이 깨져도
다른 쪽은 그대로 나가야 하기 때문이다.

수집 원칙 (상품권시세-설계.md 10절):
  - 각 업체가 자기 홈페이지에 공시한 값만 직접 읽는다
  - 비교 사이트(시세고 등)는 절대 읽지 않는다
  - 업체당 1회 요청, 순차, 간격 1초, 재시도 없음
  - 검증을 통과하지 못한 값은 빈 칸으로 둔다
"""

import json
import os
import re
import sys
import time
import urllib.error
import urllib.request
from datetime import datetime, timedelta, timezone

try:
    sys.stdout.reconfigure(encoding="utf-8")
except Exception:
    pass

import rate_parsers as rp

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
OUT_PATH = os.path.join(BASE_DIR, "html", "rates.json")
FACE = rp.FACE

UA = ("Mozilla/5.0 (compatible; KoreaGiftcardBot/1.0; "
      "+https://koreagiftcard.co.kr) 한국상품권협회 시세 수집")

SHOPS = [
    {"key": "worldticket", "name": "월드티켓상품권", "area": "명동",
     "url": "https://www.myeongdongworldticket.co.kr/", "encoding": "utf-8"},
    {"key": "xegift", "name": "엑스이상품권", "area": "명동",
     "url": "http://xegift.co.kr/html/sub0101.php", "encoding": "utf-8"},
    {"key": "choigo", "name": "최고상품권", "area": "명동",
     "url": "https://www.choigoticket.com/html/sub0101.php", "encoding": "utf-8"},
    {"key": "meee", "name": "미래상품권", "area": "명동",
     "url": "https://www.meee.co.kr/", "encoding": "utf-8"},
    {"key": "mingren", "name": "명인상품권", "area": "명동",
     "url": "https://www.mingren.co.kr/", "encoding": "utf-8"},
    {"key": "centralgift", "name": "중앙상품권", "area": "명동",
     "url": "https://centralgift.imweb.me/", "encoding": "utf-8"},
    {"key": "citypay", "name": "시티페이", "area": "명동",
     "url": "https://city-pay.co.kr/", "encoding": "utf-8"},
    {"key": "wooticket", "name": "우천상품권", "area": "명동",
     "url": "http://www.wooticket.com/", "encoding": "euc-kr"},
    {"key": "woorist", "name": "우리에스티", "area": "명동",
     "url": "https://woorist.co.kr/happyshop/show_pricelist_woori.php", "encoding": "euc-kr"},
    {"key": "sgbaekhwajeom", "name": "상품권백화점", "area": "명동",
     "url": "http://www.xn--zf0bt1zcnd5pj69p6qc.com/", "encoding": "utf-8"},
    {"key": "gogo", "name": "고고상품권", "area": "명동",
     "url": "https://xn--299aa03ct82dtjik1sz2c.com/", "encoding": "utf-8"},
]

BRANDS = ["신세계", "롯데", "현대", "갤러리아", "AK"]

NOTE = ("각 업체가 홈페이지에 공시한 값을 수집한 참고 정보이며, 실제 거래 가격은 "
        "수량·권종·상품권 상태에 따라 다를 수 있습니다. 거래 전 해당 업체에 확인하시기 바랍니다.")


def fetch(url, encoding="utf-8"):
    """페이지 1회 조회. 실패하면 예외를 그대로 올린다(재시도 없음)."""
    req = urllib.request.Request(url, headers={
        "User-Agent": UA,
        "Accept": "text/html,application/xhtml+xml",
        "Accept-Language": "ko-KR,ko;q=0.9",
    })
    with urllib.request.urlopen(req, timeout=20) as r:
        raw = r.read()
    return raw.decode(encoding, "replace")


def collect():
    """업체별 수집. 실패한 업체는 건너뛰고 나머지는 그대로 반환."""
    shops, ok = [], 0
    for s in SHOPS:
        try:
            html = fetch(s["url"], s["encoding"])
            rows = rp.PARSERS[s["key"]](html)
            rows = [r for r in rows if rp.validate_row(r, FACE)]
            if rows:
                shops.append({"name": s["name"], "url": s["url"],
                              "area": s["area"], "rows": rows})
                ok += 1
                print(f"[수집] {s['name']}: {len(rows)}건")
            else:
                print(f"::warning::[시세] {s['name']}: 유효한 행 0건 — 페이지 구조 변경 의심")
        except Exception as e:
            print(f"::warning::[시세] {s['name']} 수집 실패: {type(e).__name__}: {e}")
        time.sleep(1.0)  # 상대 서버 예의
    return shops, {"shops_ok": ok, "shops_total": len(SHOPS)}


def build_summary(shops):
    """종류별 최저 살때 / 최고 팔때."""
    out = []
    for brand in BRANDS:
        best_buy = best_sell = None
        for s in shops:
            for r in s["rows"]:
                if r["brand"] != brand:
                    continue
                if r.get("buy") is not None:
                    if best_buy is None or r["buy"] < best_buy["price"]:
                        best_buy = {"price": r["buy"], "rate": r.get("buyRate"),
                                    "shop": s["name"]}
                if r.get("sell") is not None:
                    if best_sell is None or r["sell"] > best_sell["price"]:
                        best_sell = {"price": r["sell"], "rate": r.get("sellRate"),
                                     "shop": s["name"]}
        out.append({"brand": brand, "bestBuy": best_buy, "bestSell": best_sell})
    return out


def main():
    shops, health = collect()

    # 전멸 방지 — 절반도 못 모으면 기존 데이터를 보존하고 실패시킨다
    if health["shops_ok"] < 2:
        print(f"::error::시세 수집 업체가 {health['shops_ok']}곳뿐입니다 — "
              "기존 데이터를 보존하고 종료합니다.")
        sys.exit(1)

    prev_streak = 0
    try:
        with open(OUT_PATH, encoding="utf-8") as f:
            prev_streak = int(json.load(f).get("collect_health", {})
                              .get("shop_low_streak", 0) or 0)
    except Exception:
        pass
    low = health["shops_ok"] < health["shops_total"] / 2
    health["shop_low_streak"] = prev_streak + 1 if low else 0

    kst = timezone(timedelta(hours=9))
    result = {
        "updated_at": datetime.now(kst).strftime("%Y-%m-%d %H:%M"),
        "note": NOTE,
        "face": FACE,
        "collect_health": health,
        "brands": BRANDS,
        "summary": build_summary(shops),
        "shops": shops,
    }
    with open(OUT_PATH, "w", encoding="utf-8", newline="\n") as f:
        json.dump(result, f, ensure_ascii=False, indent=2)

    print(f"[완료] {OUT_PATH}")
    print(f"       업체 {health['shops_ok']}/{health['shops_total']}곳 · "
          f"업데이트 {result['updated_at']}")
    for s in result["summary"]:
        if s["bestBuy"]:
            print(f"       - {s['brand']}: 최저 살때 {s['bestBuy']['price']:,}원 "
                  f"({s['bestBuy']['shop']})")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m unittest tests.test_collect_rates -v`
Expected: PASS

- [ ] **Step 5: 실제 수집 1회 실행**

Run: `python collect_rates.py`
Expected: 업체 9곳 이상 수집, `html/rates.json` 생성. 실패한 업체가 있으면 그 업체 파서를 고친다.

- [ ] **Step 6: 커밋**

```bash
git add collect_rates.py tests/test_collect_rates.py html/rates.json
git commit -m "Rates: 시세 수집기 — 업체별 격리 수집·검증·rates.json 생성"
```

---

## Task 6: 페이지 `/상품권시세` + 프리렌더

**Files:**
- Create: `html/상품권시세.html`
- Modify: `collect_rates.py` (프리렌더 함수 추가)

**Interfaces:**
- Consumes: `html/rates.json`
- Produces: `build_prerender(data) -> dict[str, str]`, `apply_prerender(data)`

프리렌더 마커: `<!--PR:ratesUpdatedAt-->`, `<!--PR:ratesSummary-->`, `<!--PR:ratesMatrix-->`, `<!--PR:ratesMetaDesc-->`, `<!--PR:ratesJsonld-->`

- [ ] **Step 1: 페이지 템플릿 작성**

`html/상품권딜.html` 을 복사해 헤더·푸터·CSS 골격을 그대로 쓰고 본문만 교체한다. 반드시 포함할 것:

- `<title>백화점 상품권 시세 - 신세계·롯데·현대·갤러리아·AK 살때 팔때 | 한국상품권협회</title>`
- H1: `백화점 상품권 시세`
- 설명 문단 2~3개 (8.9절 교훈 — 표와 숫자만 있으면 검색엔진이 "가격표"로만 본다)
- 고지 문구를 표 바로 위에 눈에 띄게
- 각 업체명에 원문 링크 `rel="nofollow noopener"`
- 하단 협회 매입·상담 CTA
- 프리렌더 마커 5개
- JS 렌더러 (fetch `rates.json` → 실패 시 프리렌더 유지)

- [ ] **Step 2: 프리렌더 함수 구현**

`collect_rates.py` 에 추가. **JS 렌더러와 마크업이 1:1로 같아야 한다** — 다르면 크롤러와 사용자가 다른 화면을 본다.

```python
DEAL_PAGE = os.path.join(BASE_DIR, "html", "상품권시세.html")


def esc(s):
    return (str(s).replace("&", "&amp;").replace("<", "&lt;")
            .replace(">", "&gt;").replace('"', "&quot;"))


def won(n):
    return f"{n:,}원" if isinstance(n, int) else "—"


def build_prerender(data):
    parts = {}
    parts["ratesUpdatedAt"] = esc(data.get("updated_at", "-"))

    cards = []
    for s in data.get("summary", []):
        if not s.get("bestBuy") and not s.get("bestSell"):
            continue
        buy = s.get("bestBuy") or {}
        sell = s.get("bestSell") or {}
        cards.append(
            f'<div class="rate_card"><div class="rc_brand">{esc(s["brand"])} 상품권</div>'
            f'<div class="rc_row"><span class="rc_label">살때</span>'
            f'<b>{won(buy.get("price"))}</b>'
            f'<span class="rc_shop">{esc(buy.get("shop") or "-")}</span></div>'
            f'<div class="rc_row"><span class="rc_label sell">팔때</span>'
            f'<b>{won(sell.get("price"))}</b>'
            f'<span class="rc_shop">{esc(sell.get("shop") or "-")}</span></div></div>')
    parts["ratesSummary"] = "".join(cards)

    brands = data.get("brands", [])
    head = "".join(f"<th>{esc(b)}</th>" for b in brands)
    body = []
    for sh in data.get("shops", []):
        by = {r["brand"]: r for r in sh["rows"]}
        tds = []
        for b in brands:
            r = by.get(b)
            if not r:
                tds.append('<td class="na">—</td>')
                continue
            tds.append(f'<td><span class="buy">살 {won(r.get("buy"))}</span>'
                       f'<span class="sell">팔 {won(r.get("sell"))}</span></td>')
        body.append(
            f'<tr><th scope="row"><a href="{esc(sh["url"])}" target="_blank" '
            f'rel="nofollow noopener">{esc(sh["name"])}</a>'
            f'<span class="area">{esc(sh.get("area",""))}</span></th>{"".join(tds)}</tr>')
    parts["ratesMatrix"] = (
        f'<table class="rate_table"><thead><tr><th>상품권 샵</th>{head}</tr></thead>'
        f'<tbody>{"".join(body)}</tbody></table>')

    frag = []
    for s in data.get("summary", [])[:3]:
        if s.get("bestBuy"):
            frag.append(f'{s["brand"]} {s["bestBuy"]["price"]:,}원')
    desc = ("백화점 상품권 시세 " + "·".join(frag) + ". " if frag else "")
    desc += ("신세계·롯데·현대·갤러리아·AK 백화점 상품권의 살때·팔때 시세를 "
             "상품권 거래소별로 비교해 한국상품권협회가 매시간 자동 집계합니다.")
    parts["ratesMetaDesc"] = f'<meta name="description" content="{esc(desc)}">'

    parts["ratesJsonld"] = (
        '<script type="application/ld+json">' +
        json.dumps({
            "@context": "https://schema.org",
            "@type": "Dataset",
            "name": "백화점 상품권 시세",
            "description": desc,
            "dateModified": data.get("updated_at"),
            "creator": {"@type": "Organization", "name": "한국상품권협회"},
        }, ensure_ascii=False) + "</script>")
    return parts


def apply_prerender(data):
    with open(DEAL_PAGE, encoding="utf-8", newline="") as f:
        html = f.read()
    for name, content in build_prerender(data).items():
        pattern = re.compile(r"(<!--PR:%s-->).*?(<!--/PR:%s-->)"
                             % (re.escape(name), re.escape(name)), re.S)
        html, n = pattern.subn(lambda m, c=content: m.group(1) + c + m.group(2), html)
        if n != 1:
            print(f"[오류] 프리렌더 마커 'PR:{name}' 치환 실패(발견 {n}곳)")
            sys.exit(1)
    with open(DEAL_PAGE, "w", encoding="utf-8", newline="") as f:
        f.write(html)
    print(f"[완료] 상품권시세.html 프리렌더 갱신")
```

`main()` 끝에 `apply_prerender(result)` 를 추가한다.

- [ ] **Step 3: 렌더 파리티 확인**

Run: `python collect_rates.py`
그다음 로컬 서버로 열어 JS 렌더 결과와 프리렌더 결과가 같은지 확인한다:

```bash
# .claude/launch.json 의 deals-preview 서버 사용
```

브라우저에서 `상품권시세.html` 을 열고 콘솔 에러가 없는지, 표 행 수가 프리렌더와 같은지 확인한다.

- [ ] **Step 4: 커밋**

```bash
git add "html/상품권시세.html" collect_rates.py
git commit -m "Rates: /상품권시세 페이지 + 프리렌더(JS 렌더러와 파리티 유지)"
```

---

## Task 7: 메뉴 17개 파일 + sitemap + 내부링크

**Files:**
- Modify: `html/*.html` 17개 (메뉴)
- Modify: `html/sitemap.xml`
- Modify: `html/상품권딜.html` (상호 링크)

- [ ] **Step 1: 메뉴 삽입 스크립트 작성 후 실행**

모든 페이지의 메뉴에 아래 패턴이 파일당 1회 있다:

```html
<li class="deal-menu">
    <a href="/상품권딜">상품권 핫딜</a>
</li>
```

바로 뒤에 시세 메뉴를 넣는다:

```bash
python - <<'PY'
import glob, io, re
OLD = '<li class="deal-menu">\n                        <a href="/상품권딜">상품권 핫딜</a>\n                    </li>'
n = 0
for p in glob.glob('html/*.html'):
    s = io.open(p, encoding='utf-8', newline='').read()
    if 'href="/상품권시세"' in s or 'href="/상품권딜"' not in s:
        continue
    # 들여쓰기를 보존해 삽입
    m = re.search(r'([ \t]*)<li class="deal-menu">\s*<a href="/상품권딜">상품권 핫딜</a>\s*</li>', s)
    if not m:
        print('패턴 불일치:', p); continue
    indent = m.group(1)
    add = (f'\n{indent}<li class="rate-menu">\n'
           f'{indent}    <a href="/상품권시세">상품권 시세</a>\n'
           f'{indent}</li>')
    s = s[:m.end()] + add + s[m.end():]
    io.open(p, 'w', encoding='utf-8', newline='').write(s)
    n += 1
print(f'{n}개 파일 수정')
PY
```

Expected: `17개 파일 수정`

- [ ] **Step 2: 삽입 검증**

```bash
grep -l 'href="/상품권시세"' html/*.html | wc -l   # 17 이어야 함
grep -c 'href="/상품권시세"' html/index.html        # 1 이어야 함 (중복 삽입 아님)
```

- [ ] **Step 3: sitemap 등록**

`html/sitemap.xml` 의 `/상품권딜` 항목 뒤에 추가한다 (URL 인코딩 형태를 기존 항목과 맞춘다):

```xml
  <url>
    <loc>https://koreagiftcard.co.kr/%EC%83%81%ED%92%88%EA%B6%8C%EC%8B%9C%EC%84%B8</loc>
    <lastmod>2026-08-12</lastmod>
    <changefreq>hourly</changefreq>
    <priority>0.9</priority>
  </url>
```

- [ ] **Step 4: 상호 내부링크**

`html/상품권딜.html` 의 FAQ 아래에 시세 페이지 링크를 한 줄 넣고, 시세 페이지에도 핫딜 링크를 넣는다. 앵커텍스트는 키워드 형태로: `백화점 상품권 시세 비교`, `상품권 핫딜 할인율`.

- [ ] **Step 5: 커밋**

```bash
git add html/
git commit -m "Rates: 메뉴 17개 파일 추가 + sitemap 등록 + 핫딜 페이지 상호 링크"
```

---

## Task 8: 워크플로 스텝 + 건강 점검

**Files:**
- Modify: `.github/workflows/collect-deals.yml`

- [ ] **Step 1: 수집 스텝 추가**

`딜 수집 실행` 스텝 뒤, `변경 시 커밋 & 푸시` 앞에 넣는다:

```yaml
      - name: 시세 수집 실행
        # 시세 수집이 실패해도 핫딜 갱신은 계속돼야 하므로 continue-on-error.
        # 수집기 자체가 업체별로 실패를 격리하지만, 전체가 죽는 경우까지 대비한다.
        continue-on-error: true
        run: python collect_rates.py
```

- [ ] **Step 2: 커밋 대상에 추가**

`git add` 줄에 시세 산출물을 더한다:

```yaml
          git add html/deals.json "html/상품권딜.html" html/sitemap.xml html/rates_history.json html/rates.json "html/상품권시세.html"
```

- [ ] **Step 3: 건강 점검 확장**

기존 `자동 갱신 건강 점검` 스텝의 `run` 끝에 추가:

```bash
          rlow=$(jq -r '.collect_health.shop_low_streak // 0' html/rates.json 2>/dev/null || echo 0)
          rok=$(jq -r '.collect_health.shops_ok // 0' html/rates.json 2>/dev/null || echo 0)
          echo "시세 수집 업체 ${rok}곳 / 절반 미만 연속 ${rlow}회"
          if [ "$rlow" -ge 3 ]; then
            echo "::error::시세 수집 업체가 ${rlow}회 연속 절반 미만입니다 — 파서 점검 필요."
            exit 1
          fi
```

- [ ] **Step 4: 커밋 & 수동 실행 검증**

```bash
git add .github/workflows/collect-deals.yml
git commit -m "Rates: 워크플로에 시세 수집 스텝·건강 점검 추가"
git push
gh workflow run collect-deals.yml
```

로그에서 `[수집]` 라인이 업체별로 찍히고 `rates.json` 이 커밋되는지 확인한다.

---

## Task 9: 통합 검증 및 문서화

- [ ] **Step 1: 전체 테스트**

```bash
python -m unittest discover -s tests -v
```
Expected: 전부 PASS

- [ ] **Step 2: 실제 페이지 검증**

로컬 서버로 `/상품권시세.html` 을 열어 확인한다:
- 표 행 수 = `rates.json` 의 `shops` 개수
- 콘솔 에러 없음
- 모바일(375px)에서 가로 스크롤 없음 — 5열 표라 `overflow-x: auto` 컨테이너 필수
- 프리렌더와 JS 렌더 결과 일치

- [ ] **Step 3: 값 육안 검증**

`rates.json` 의 값 몇 개를 실제 업체 사이트와 대조한다. **하나라도 다르면 배포하지 않는다.**

- [ ] **Step 4: 문서 갱신**

`상품권핫딜-프로젝트정리.md` 에 시세 페이지 절을 추가한다: 수집 대상 11곳, 검증 규칙, 파서가 깨졌을 때 대처법(픽스처 갱신 → 테스트 → 파서 수정), 운영 원칙(비교 사이트 수집 금지·중단 요청 시 즉시 제외).

- [ ] **Step 5: 커밋 & 배포**

```bash
git add -A
git commit -m "Docs: 상품권 시세 페이지 운영 문서"
git push
```

---

## Self-Review 결과

**스펙 커버리지**: 설계 문서 3~11절이 Task 1~9 에 각각 대응한다. 12절(리스크)은 Task 5 의 전멸 방지 가드, Task 8 의 건강 점검, Task 9 의 육안 검증으로 흡수했다.

**주의가 필요한 지점**:
- Task 2~4 의 파서 코드는 조사 결과를 근거로 작성한 **초안**이다. 실제 픽스처와 다르면 픽스처가 우선이며, 각 태스크 Step 5 에 그 지침을 명시했다.
- `parse_sgbaekhwajeom` 과 `parse_gogo` 는 표 구조가 아니라 가장 불확실하다. 픽스처 확인 후 구조가 다르면 파서를 다시 써야 할 수 있다.
- 액면가 선별은 `validate_row` 의 범위 검사(90~102%)가 겸한다. 5만·50만원권은 자동으로 걸러진다.
