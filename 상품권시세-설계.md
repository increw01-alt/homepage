# 상품권 시세 페이지 설계 (2026-08-12)

## 1. 목적

백화점 상품권(신세계·롯데·현대·갤러리아·AK)의 **오프라인 매매 시세를 한눈에** 보여주는 페이지를 `/상품권시세` 에 신설한다. 참조 서비스는 시세고(sisego.com).

기존 `/상품권딜` 과의 차이:

| | /상품권딜 (기존) | /상품권시세 (신규) |
|---|---|---|
| 데이터 | 오픈마켓 **온라인 구매 할인율** | 상품권 거래소 **매매 시세** |
| 방향 | 살 때만 | **살때 + 팔때** |
| 출처 | 11번가 상품 페이지 | 명동 상품권 거래소 11곳 |
| 성격 | 최저가 딜 | 시장 시세 비교 |

두 페이지는 성격이 달라 **합치지 않는다.** 섞으면 이용자가 "온라인 할인가"와 "거래소 매입가"를 혼동한다.

## 2. 조사 결과 (2026-08-12)

시세고에 실린 업체 14곳의 시세 페이지를 전수 조사했다.

- **11곳이 `high`** — 시세가 서버 HTML 에 그대로 있고 표 구조가 고정이라 정규식·선택자로 안정적으로 파싱 가능. JS 실행이나 헤드리스 브라우저 불필요.
- **robots.txt 로 수집을 막는 곳은 없었다.** 대부분 `Allow: /` 이고, 일부는 로그인·장바구니 경로만 차단한다(우리가 읽는 시세 페이지는 허용).
- 제외 3곳: 어즈티켓(583KB·운영자 수동 입력표), 퍼스트티켓, 와이제이기프트(값이 HTML 에 없음).

**모든 업체가 `금액 = 액면가 × (1 − 할인율)` 을 정확히 지킨다.** 이 항등식이 파싱 자가검증의 핵심이 된다.

## 3-A. 실제 운영 결과 (2026-08-12 구현 완료)

조사에서 `high` 로 판정한 11곳 중 **실제 운영에 남은 것은 7곳**이다. 조사(로컬·국내 IP)와 운영(GitHub Actions·해외 IP)의 환경 차이 때문이다.

| 상태 | 업체 | 사유 |
|---|---|---|
| **운영 중 (8)** | 월드티켓·엑스이·최고상품권·**미래상품권**·중앙상품권·시티페이·상품권백화점·고고상품권 | 고고상품권은 간헐적으로 403 이 나지만 격리돼 있어 나머지에 영향 없음 |
| 제외 | 명인상품권, 우천상품권, 우리에스티 | **해외 데이터센터 IP 차단.** 정상 응답이 27~82KB 인데 765~1,600자짜리 본문에 title 도 브랜드 언급도 없는 페이지가 돌아온다. 프록시 우회는 하지 않는다 |

**미래상품권 복구(2026-08-12)**: https 는 인증서 만료로 검증에 실패하지만 `http://meee.co.kr/` 로는 정상 수신된다. **인증서 검증을 끄는 것보다 http 가 낫다** — 검증을 끄면 암호화된 척하면서 실제로는 확인하지 않는 상태가 되어 더 나쁜 착각을 준다. http 는 평문임이 명확하고, 읽는 값이 공개 시세뿐이며 4중 검증을 통과해야 채택된다. 이미 엑스이·상품권백화점을 http 로 읽고 있어 관행과도 일치한다.

**제외된 업체의 파서는 `rate_parsers.py` 에 그대로 두고 픽스처 테스트로 계속 검증한다.** 대부분 일시적 사유라 파서를 지우면 복구할 때 처음부터 다시 만들어야 한다. 국내에서 도는 수집기를 따로 두게 되면 3곳을 즉시 되살릴 수 있다.

**진단이 가능했던 이유**: 수집 0건일 때 응답 지문(크기·title·브랜드 언급 횟수)을 로그에 남기도록 해 두었다. "유효한 행 0건"만으로는 구조 변경과 차단을 구분할 수 없다.

## 3. 수집 대상 (조사 시점 11곳)

| 업체 | 대상 URL | 크기 | 인코딩 | 파싱 핵심 |
|---|---|---:|---|---|
| 월드티켓상품권 | `myeongdongworldticket.co.kr/` | 9KB | UTF-8 | `div.guide-table table tbody tr`, td 3열 |
| 엑스이상품권 | `xegift.co.kr/html/sub0101.php` | 9KB | UTF-8 | 단일 table, td[1]=매입(`.blue`), td[2]=판매(`.red`) |
| 최고상품권 | `choigoticket.com/html/sub0101.php` | 16KB | UTF-8 | `<input _amt1 _amt2 _item>` 속성에서 직접 추출(권장) |
| 명인상품권 | `mingren.co.kr/` | 27KB | UTF-8 | **`div.sise_box.box0.active` 로 스코프 한정 필수** (box0~6 중복 존재) |
| 미래상품권 | `meee.co.kr/` | 27KB | UTF-8 | `table tbody.giftcard_list > tr`, `td.tit`=종류명 |
| 상품권백화점 | `xn--zf0bt1zcnd5pj69p6qc.com/` | 40KB | UTF-8 | `li.sct_li` 안 `div.sct_txt` 블록 |
| 우리에스티 | `woorist.co.kr/happyshop/show_pricelist_woori.php` | 82KB | **EUC-KR** | 시세표 전용 페이지로 **URL 교체**(기존 sub_list 는 살때만 있음) |
| 우천상품권 | `wooticket.com/` | — | **EUC-KR** | 레거시 테이블 레이아웃, 전 품목 정적 표 |
| 시티페이 | `city-pay.co.kr/` | — | UTF-8 | WordPress wpDataTables, `table#table_2 tbody tr` |
| 중앙상품권 | `centralgift.imweb.me/` | 565KB | UTF-8 | 페이지 내 `<table>` 1개뿐, td 4열(명/액면/매입/판매) |
| 고고상품권 | `xn--299aa03ct82dtjik1sz2c.com/` | 869KB | UTF-8 | `div.tab-editor[editor-name]` 6블록 분리 후 각 table |

총 전송량 약 1.7MB/회. 매시간이면 하루 40MB 수준으로 부담 없다.

**주의 사항**
- 우천상품권·우리에스티는 **EUC-KR** 이므로 `resp.content.decode('euc-kr')` 로 먼저 변환한다. UTF-8 로 읽으면 한글이 깨져 종류명 매칭이 전부 실패한다.
- 명인상품권은 같은 상품명이 여러 박스에 중복 등장한다. `box0.active` 로 좁히지 않으면 **조용히 틀린 값**이 들어간다.
- 최고상품권·엑스이는 전용 시세 페이지가 메인보다 가벼우므로 그쪽을 쓴다.

## 4. 아키텍처

`collect_deals.py` 에 넣지 않고 **`collect_rates.py` 를 신설**한다.

- `collect_deals.py` 는 이미 1,100줄이고, 온라인 딜 수집·프리렌더·시세 이력·사이트맵·IndexNow 를 모두 담고 있다. 여기에 11개 파서를 더하면 한 파일이 두 개의 서로 다른 시스템을 갖게 된다.
- 더 중요한 이유는 **장애 격리**다. 시세 수집이 깨져도 핫딜 페이지는 영향받지 않아야 하고, 그 반대도 마찬가지다.

```
collect_rates.py
  ├ SHOPS[]            업체 메타(이름·URL·지역·인코딩·파서 키)
  ├ PARSERS{}          업체별 파서 함수. HTML → [{brand, face, buy, sell}]
  ├ validate()         공통 검증(항등식·범위·살때>팔때)
  ├ build_summary()    종류별 최저 살때 / 최고 팔때
  ├ build_prerender()  html/상품권시세.html 정적 렌더
  └ main()             수집 → 검증 → rates.json → 프리렌더
```

**파서는 업체당 하나의 독립 함수**로, 실패해도 그 업체만 빠진다. 한 곳이 사이트를 개편해도 나머지 10곳은 정상 노출된다.

## 5. 데이터 모델 (`html/rates.json`)

```json
{
  "updated_at": "2026-08-12 15:00",
  "note": "각 업체가 홈페이지에 공시한 값을 수집한 참고 정보입니다...",
  "face": 100000,
  "collect_health": { "shops_ok": 11, "shops_total": 11, "shop_zero_streak": 0 },
  "brands": ["신세계", "롯데", "현대", "갤러리아", "AK"],
  "summary": [
    { "brand": "신세계",
      "bestBuy":  { "price": 96700, "rate": 3.3, "shop": "최고상품권" },
      "bestSell": { "price": 96600, "rate": 3.4, "shop": "월드티켓상품권" } }
  ],
  "shops": [
    { "name": "최고상품권", "url": "https://www.choigoticket.com/", "area": "명동",
      "rows": [ { "brand": "신세계", "buy": 96700, "buyRate": 3.3,
                  "sell": 96600, "sellRate": 3.4 } ] }
  ]
}
```

**용어 정의(반대로 넣기 쉬우므로 고정한다)**
- `buy` = **고객이 사는 값** = 업체 표기의 "판매가"
- `sell` = **고객이 파는 값** = 업체 표기의 "매입가"
- 항상 `buy > sell` 이다(업체 마진). 이 부등식이 매핑 오류 검사에 쓰인다.

## 6. 검증 규칙 (틀린 시세를 내보내지 않기 위한 방어)

파싱된 각 값은 다음을 **모두** 통과해야 채택된다. 하나라도 실패하면 그 행을 버린다.

1. **항등식** — `|금액 − 액면가 × (1 − 할인율/100)| ≤ 액면가 × 0.01`
   업체가 금액과 할인율을 함께 표기하므로 서로를 교차검증할 수 있다. 컬럼 정렬이 어긋나면 즉시 걸린다.
2. **범위** — 금액이 액면가의 90~102% 안에 있어야 한다.
3. **부등식** — `buy > sell`. 뒤집혀 있으면 매입/판매 컬럼을 반대로 읽은 것이다.
4. **종류 화이트리스트** — 신세계·롯데·현대·갤러리아·AK 로 정규화되지 않는 행은 버린다(구두·주유·문화 상품권 등 혼입 방지).

행이 전부 버려진 업체는 해당 업체만 목록에서 제외한다.

## 7. 화면 (`/상품권시세`)

기존 `/상품권딜` 의 레이아웃·CSS 를 재사용해 사이트 톤을 유지한다.

1. **히어로** — "백화점 상품권 실시간 시세" + 마지막 갱신 시각
2. **종류별 요약 카드** — 신세계·롯데·현대·갤러리아·AK 각각 최저 살때 / 최고 팔때 + 해당 업체명
3. **업체별 매트릭스 표** — 행=업체(11곳), 열=상품권 5종. **10만원권 기준**으로 통일한다. 한 칸에 살때·팔때를 위아래로 함께 표기하고(예: `살 96,700 / 팔 96,600`), 표 상단 토글로 "살때만/팔때만" 보기를 전환할 수 있게 한다 — 기본은 둘 다 표시. 각 행에 업체명과 원문 링크(`rel="nofollow noopener"`).
4. **고지 문구**(표 바로 위, 눈에 띄게) — "각 업체가 홈페이지에 공시한 값을 수집한 **참고 정보**이며, 실제 거래 가격은 수량·권종·상품권 상태에 따라 다를 수 있습니다. 거래 전 해당 업체에 확인하시기 바랍니다."
5. **협회 CTA** — 매입·할부 구매 상담으로 연결

액면가는 10만원권 고정. 업체별로 취급 액면가가 달라 여러 액면가를 한 표에 넣으면 빈 칸이 급증한다.

## 8. 갱신·감시

- 기존 `collect-deals.yml` 매시간 워크플로에 **스텝을 추가**한다(별도 크론을 늘리지 않는다). 같은 `concurrency` 그룹이라 커밋 경합도 없다.
- 업체당 1회 요청, 순차 진행하며 요청 사이 간격을 둔다. 실패해도 재시도하지 않는다(상대 서버 부담 방지).
- **감시**: `shops_ok` 가 전체의 절반 미만이면 `::warning::`, 3회 연속이면 워크플로를 실패시켜 GitHub 알림. 핫딜 쪽 `live_zero_streak` 와 같은 패턴이다.
- 커밋·푸시 뒤에 점검 스텝을 두어, 알림이 떠도 사이트 갱신은 계속되게 한다.

## 9. SEO

- 별도 페이지이므로 `상품권 시세`·`백화점 상품권 시세` 키워드를 title 에서 정조준한다.
- title: `백화점 상품권 시세 - 신세계·롯데·현대·갤러리아·AK 살때 팔때 | 한국상품권협회`
- 프리렌더(정적 HTML 굽기), `Table`·`BreadcrumbList` 구조화 데이터, sitemap 등록, `/상품권딜` 과 상호 내부링크.
- 8.9절에서 배운 대로 **설명 문장을 반드시 넣는다** — 표와 숫자만 있으면 검색엔진이 "가격표"로만 본다.

## 10. 운영 원칙 (지킬 것)

- **시세고를 포함해 남이 모아 놓은 비교 사이트는 절대 수집하지 않는다.** 그건 타인의 DB 를 복제하는 것이다. 각 업체가 자기 홈페이지에 공시한 값만 직접 읽는다.
- robots.txt 를 존중한다. 수집 금지로 바뀐 업체는 즉시 제외한다.
- User-Agent 에 협회임을 밝히고 연락 가능한 정보를 남긴다.
- 출처(업체명)와 원문 링크를 항상 함께 노출한다. 시세만 떼어 보여주지 않는다.
- 값이 검증을 통과하지 못하면 **빈 칸으로 둔다.** 추정값을 채우지 않는다.
- 업체가 표시를 중단하거나 삭제를 요청하면 즉시 제외한다.

## 11. 구현 순서

1. `collect_rates.py` 뼈대 + 공통 검증 + rates.json 생성
2. 파싱이 쉬운 3곳(월드티켓·엑스이·최고상품권)으로 파이프라인 검증
3. 나머지 8곳 파서 추가 (EUC-KR 2곳, 스코프 한정 1곳 주의)
4. `html/상품권시세.html` 템플릿 + 프리렌더 + JS 렌더러(파리티 유지)
5. 메뉴 추가 — `<li class="deal-menu">` 뒤에 시세 메뉴 삽입, **17개 파일 일괄**
6. sitemap 등록, 구조화 데이터, 내부링크
7. 워크플로 스텝 + 건강 점검 추가
8. 로컬 검증(전 업체 파싱 결과 육안 확인) → 배포

## 12. 리스크

| 리스크 | 대응 |
|---|---|
| 업체 사이트 개편으로 파서 깨짐 | 업체별 격리 + 검증 실패 시 해당 업체만 제외 + 절반 미만이면 알림 |
| 틀린 시세 노출로 협회 신뢰 훼손 | 4중 검증 + "참고 정보" 고지 + 출처·원문 링크 |
| 업체의 수집 중단 요청 | 즉시 제외. 연락 가능한 UA 표기 |
| 경쟁 업체 노출 부담 | 협회 매입·상담 CTA 를 함께 배치. 시세 비교는 이용자 편익, 전환은 협회 강점(안전·할부)으로 |
| 값이 묵어 신선도 저하 | 갱신 시각 표기. 업체 페이지에 타임스탬프가 없으므로 "수집 시각" 임을 명시 |
